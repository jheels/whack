const ProgressPopup = ({ onClose }) => {
    return (
        <div>
            <h1>h</h1>
            <button onClick={onClose}>Go Back</button>
        </div>
    );
};

export default ProgressPopup;
